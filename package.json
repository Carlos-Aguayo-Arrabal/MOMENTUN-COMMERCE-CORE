{
  "name": "Tienda-maestra",
  "version": "0.1.0",
  "private": true,
  "description": "Tienda-maestra - Generated with SaaS Factory",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "echo \"No backend configured\"",
    "dev:all": "npm run dev:frontend",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "echo \"No build step for backend\"",
    "install:all": "npm install && cd frontend && npm install",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:backend": "echo \"No lint for backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
